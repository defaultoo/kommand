local Commands = {}
Commands["kommand"] = { 
	{"dex", "<function>, Opens the built in dex explorer utilising lib:Dex(). It installs and opens just like a normal module."}
	{"remotespy", "<function>, Opens the built in remotespy utilising lib:RemoteSpy(). It installs and opens just like a normal module."}	
}
Commands["settings"] = { 
	{"blur", "<setting>, <boolean> - Changes the background blur seen on the UI"}
}
Commands["system"] = { 
	{"
	{"console", "<function> - Opens the default Roblox Developer Console"}
}
Commands["special"] = { 
	{"install", "<function>, <value> - Fetches, downloads, and executes a module from the GitHub repo"},	
	{"executemodule", "<function>, <value> - Fetches and executes a module from the GitHub repo"},	
	{"serverhop/safehop", "<function> - Disconnects and rejoins the player into potentially a different server"}
}
